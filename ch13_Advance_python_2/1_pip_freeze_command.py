

# RUN 'pip freeze' command in terminal --> it will show all installed packages

# 'pip freeze > requirement.txt' - this command will generate requirements.txt which contains info on installled packages
#  to install the same packages again ---- 'pip install -r requirements.txt'----- this will install all the packages mentioned in the file (this is used when virtual env is created)